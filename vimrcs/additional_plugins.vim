" Additional plugins can be added that are not included in the default config. It is used for plugins do not want 
" installed by default such as Valloric/YouCompleteMe which has more dependencies than other plugins and would
" not easily install on every system the same. Thus, those plugins must be installed manually by the user for 
" each deployment and can be placed here to avoid conflicts. 


Plugin 'Valloric/YouCompleteMe'
